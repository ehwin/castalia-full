# Castalia 记忆管线全景报告（2026-09-12 18:26 实测）

> 覆盖：写入 → 存储 → 反思 → 检索 → 前端渲染 全链条。
> 所有数字当次实测（md5 矩阵 / SQLite 只读 / 浏览器探针），附录 `_pipeline_report_matrix.json`。
> 姊妹篇：《审记报告-20260912-星图与沉淀管线.md》（问题清单 P1-P10），本篇聚焦**管线如何工作、哪里断**。

---

## 〇、一图看全链

```
【写入层】                    【存储层】                     【反思层】                    【检索层】         【前端层】
LobeHub/AIRI 客户端
   │ auto_process(sessionId)                                    (三通道 LLM)
   ├─① saveConversationTurn ──→ 总库 conversation 行 ─┐
   │    ⚠B2: is_active=1 直写总库                     ├─→ maybeDigest ──→ reflect_auto ──→ store 检索 ──→ /api/graph ──→ 星图 3344/3345/3346
   └─② buffer.onNewMessage ─→ [纯内存] ─→ 增量反思 ─→ promoteToProject ──→ 总库
        ⚠B1/B3/B4(见§二)                          ⚠A1: category 硬码,无三闸

引擎位（谁在哪跑）:
  Hermes(:5050 经 MCP)   → run/Castalia dist      = Full 产物（同源矩阵✓）
  LobeHub(HTTP 桥 3310)  → lobehub-run dist        = Full 产物（同源矩阵✓）
  AIRI(unified-proxy)    → run/Castalia-Anima dist  = Anima 产物（独有 5 情感文件✓）
  三端星图 web           = Full web 单一源（md5 3bc366a8 四端一致✓）

三通道 LLM:
  ① triage(分拣/增量反思)  ② reflect(消化/深度)  ③ embedding(1024d, Yuan-EB :11436)
  keys: keys.enc(各盘独立), admin 界面三通道可配
```

---

## 一、管线各段现状（正常运转的部分）

### 1. 存储层（memdir 分库）— 健康 ✓
```
各盘 memory/<project>/<memType>/memory.sqlite + global.sqlite(指令/项目注册)
Hermes 盘: default+hermes+shared+reflect    LobeHub 盘: lobehub+shushu+reflect+shared
AIRI 盘: airi(单库)                          keys.enc/keys.key 每盘独立 ✓
```
- 三仓 src↔dist 全同步 ✓（附录 lag 矩阵）
- `sync_frontend.py` 单一源机制运转 ✓（index/graph.js 四端 md5 全等）
- 引擎 dist 六部署位：Full/Lite/Anima 源码仓 + 三实例，同源对全部咬合 ✓

### 2. 检索层（search.ts）— 逻辑健康，但被坏数据拖累
- decay 分级（critical 永不衰减 / 时效类 10 天半衰期）三仓已齐 ✓
- character_id 硬过滤已移除（08-17），project 为隔离键 ✓
- 问题不在检索：是上游喂进来的 `session_promoted` 假类别与 1203 字长文让 topK 质量劣化（症状：LobeHub 域检索霸榜旧闻）

### 3. 前端层 — 单一源运转 ✓
- Full web = 唯一源；`scripts/sync_frontend.py` 推三端 + `--check` 校验
- 星图 v1.18.3：星云视图（纯 PCA 嵌入）+ 星系视图（辅助）双态
- 实测（3 小时前）：AIRI 域 20 管干线穿模率 0、联邦 34 管

---

## 二、管线断点（按数据流顺序）

### 写入段 ⚠
| 编号 | 断点 | 位置 | 现状 |
|------|------|------|------|
| B1 | **promote 直通总库**（无隔离带/无三闸） | store.ts `promoteToProject` | LLM 一轮判生即 INSERT；仅文本精确全等去重 |
| B2 | **对话原文 is_active=1 直写总库** | store.ts `saveConversationTurn` | 本应走"分隔带"（审计可见/检索不可见） |
| B3 | **会话历史仅存内存** | buffer.ts `SessionMemoryBuffer` | 进程重启即丢；违背"都应落库"意图 |
| B4 | **同轮消息双通道重复反思** | autoProcessor.ts | saveConversationTurn + buffer 双入口喂同一内容 |
| B5 | mood 前缀拼正文 | saveConversationTurn | `[mood: …]用户:…` 污染嵌入 |
| B6 | digest 无 per-project 锁 | digest.ts | 多库共享消化节奏 |

### 反思段 ⚠
| 编号 | 断点 | 后果 |
|------|------|------|
| A1 | **promote 产物 category 硬码 `session_promoted`** | 语义层被假类别覆盖；星图 category 星座挤爆（LobeHub 盘 207/341=60%） |
| A2 | Full/Lite 反思 prompt 缺 category 赋义段（Anima 有） | 两盘反思产物无语义类别 → A1 只能硬码；三仓同步历史事故 |
| A3 | `classifyMemTypeLLM` 孤儿（autoClassify 未接线） | 主写入路径不经分拣；"先分四类"只在反思侧成立 |

### 数据灾情（现有库存量，全实证）
| 盘 | 灾情 |
|----|------|
| LobeHub | 207 条 session_promoted（60%）；41 条 conversation 长文（中位 1203 字/最长 2850）；11 条 AIRI 工程内容跨盘（8/23 16:33 单批 189 条尖峰） |
| AIRi | 干净（108 条，category 十类均衡）——因 Anima 带赋义规则 |
| Hermes | 量小，暂无显灾；但 B1-B6 断点同样存在（灾情未到只是量未到） |

### 前端段 ⚠（轻，2 处）
| 编号 | 断点 |
|------|------|
| F1 | 星图 `/api/graph` 只读 `is_active=1` —— 正确；但 B2 把对话原文置 1，等于分隔带从未生效（修复 B2 后前端零改动自动变干净） |
| F2 | 联邦图 34 管第三方穿模 20.6%（LobeHub 域星座物理重叠场景，干线掠过邻团缝隙——可后续"门位沿切平面外推"精调） |

---

## 三、因果主链（一张读）

```
B4 双通道 + B3 内存丢史 ──┐
B1 无闸 promote ←────────┤
A2 缺赋义(Anima 独有) ←──┼──→ A1 硬码 session_promoted ──→ 8/23 批量尖峰 189 条
                         │         │
B2 对话原文直写 ──────────┘         └──→ 数据灾(LobeHub 盘)
                                          │
                                          ├─→ 检索: 旧闻霸榜/长文稀释 topK
                                          ├─→ 星图: LobeHub 域巨团糊 / AIRI 域干净(对照组)
                                          └─→ 星云 PCA: 长文嵌入拉偏全库主轴
```

---

## 四、修复方案（已对齐，按依赖序）

### 第一波（引擎，一处函数为主）
1. **分库三带**（用 source+is_active 表达，不加表）：总库 / `conversation_log` 带（is_active=0）/ `session_ephemeral` 带（is_active=0）
2. **promoteToProject 三闸**：密度（>240 字或低信息密度拒）/ 归属（跨盘专名出现即隔离）/ delta（与库存 cosine>0.9 近重复拒）+ category 交 LLM（出 A1 硬码）
3. **saveConversationTurn 分隔带化** + mood 前缀入 metadata
4. buffer 落库（session_ephemeral 带）+ autoProcess 去双通道
5. digest per-project 锁；saveMemory 接 autoClassify
6. 反思 prompt 补 category 赋义（回灌 Anima 已验证规则）
7. 三仓 sync → build → dist 三实例 → 重启（托盘链）

### 第二波（存量清创，写库前备份）
8. 备份三盘（backup API 一致性拷贝）
9. 207 条 session_promoted：真值重派 category（LLM 复扫）/ 空话软删；41 条长文压缩 ≤200 字；11 条 AIRI 杂质软删+隔离标记
10. LobeHub 盘 `reflect_batch_embed` 重建向量 → 实测检索（"金四局 紫微"类查询应命 中 decision 条目）

### 第三波（交付收尾）
11. 前端六轮 commit（v1.17.4→v1.18.3）
12. 探针端口分离（4344-4346，防杀服务复发）
13. git 远程收口（等 A/B/C）
14. 本报告姊妹篇未尽事项见其 §三

---

## 五、附录
- md5 矩阵：`_pipeline_report_matrix.json`（当次快照：前端四端 3bc366a8 等值、引擎六位部署同源、src↔dist 全同步）
- 数据灾情详表：`_audit_report_data.json`
- 复现方法：见姊妹篇 §四
