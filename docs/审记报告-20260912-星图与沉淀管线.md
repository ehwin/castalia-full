# Castalia 全链路审计报告（2026-09-12）

> 本轮会话：星图 v1.14→v1.18 迭代过程中暴露的记忆管线与数据问题。
> 方法：全部结论经源码审读（buffer.ts/triage.ts/store.ts/autoProcessor.ts/digest.ts/promoteToProject）
> + SQLite 实证（只读）+ 浏览器探针（puppeteer/Edge）交叉验证，非推测。
> 数据附录：`_audit_report_data.json`（按 盘×库×memType 的全文统计）。

---

## 一、问题总表（按严重度排序）

| # | 问题 | 层 | 证据 | 影响 |
|---|------|----|------|------|
| P1 | **promote 产物 category 硬码 `session_promoted`** | 引擎(store.ts:680) | INSERT 硬编码不取 LLM 语义 | 207 条假类别占 LobeHub 盘 60%；星图 category 星座层被单一巨团挤爆；AIRI 盘因 Anima 反思规则而有真 category 所以图正常 —— 两盘图质差异的直接根源 |
| P2 | **对话原文以 is_active=1 直接进总库** | 引擎(store.ts saveConversationTurn) | `用户:…尤诺:…` 整段入库，41 条中位 1203 字 | 总库/检索/嵌入/星图全部被原始对话污染；这正是"临时沉淀进了总图" |
| P3 | **promote 无质量闸门**（无密度闸/无归属闸/无 delta 闸） | 引擎(promoteToProject) | 唯一防线=文本精确全等去重 | 1203 字长文照 promote；AIRI 工程旧闻 9 条经 LobeHub 通道重复入库（跨项目名混库的机制）；6→AIRI、AI 决策全进 lobehub 总库 |
| P4 | **临时层不落库 + 会话历史仅存内存** | 引擎(buffer.ts) | SessionMemoryBuffer 纯内存单例，flush 后 history 清空 | 进程重启即丢；无隔离观察带，LLM 一轮判生即直写总库；与"都应落库"设计意图相悖 |
| P5 | **digest 热闸无 project 维度** | 引擎(digest.ts) | lastDigestTime 模块级单变量；计数无 project 参数 | 多库共享消化节奏，互相错乱 |
| P6 | **mood 前缀拼进检索正文** | 引擎(saveConversationTurn) | `[mood: …]用户:…` | 嵌入向量与检索吃到噪声 |
| P7 | **同轮消息双通道重复反思** | 引擎(autoProcessor.ts) | saveConversationTurn（convers_log）+ buffer.onNewMessage（同一内容再进 incremental 反思 transcript） | 同一内容产生两次 LLM 判断、两份入库路径；8/23 批量反思 189 条尖峰的产出结构 |
| P8 | **classifyMemTypeLLM 成孤儿（autoClassify 未接线）** | 引擎(triage.ts) | 函数存在但零调用方；saveMemory 的 memType 全靠调用方传，不传归 general | "先分四类再落库"仅在反思产物侧成立，主写入路径未走分拣；记忆库档案已预告此坑（2026-09-05 档案："autoClassify 治本方案未实施"） |
| P9 | **跨仓 dist/源码漂移链**（历史遗留，本周加剧） | 交付 | Full 仓 HEAD=wip 试验稿；工作区 v1.18 未提交；Anima 仓 dist 一度倒灌风险 | 见 §三 |
| P10 | **GitHub 远程历史被改写未收口** | 交付 | origin/main(b2f9c02→d80752e forced-update)，两副本分叉、净缺 6 修复未推 | 至今悬置 |

---

## 二、成因链（一页读完）

```
设计意图（Claude 方式落实）:
  入站 → 四类封闭 memType（子分类自由不设限）→ 总库
      ↘ 会话滚动(临时层，应落库不进总图)

实际代码（三处旁门造成 P1-P8）:
  A. promoteToProject      → category 硬码 'session_promoted'        (P1)
  B. saveConversationTurn  → 对话原文 is_active=1 直写总库            (P2/P6)
  C. buffer                → 纯内存 + LLM 单轮判生直写总库            (P3/P4)
  D. autoProcessor         → 双通道重复反思                           (P7)
  E. digest/saveMemory     → 无 project 锁 + autoClassify 缺线        (P5/P8)

数据成灾现场（LobeHub 盘实证）:
  8/23 16:33 单批 189 条 promote 尖峰
  session_promoted 类别 207 条（占盘 60%）
  shushu::conversation 41 条，中位 1203 字（最长 2850）
  AIRI 名字内容 11 条进 lobehub/reflect 库（PR#1957/五层分工等旧闻）
  → 星图: LobeHub 域巨团糊（P1+P2 主料），AIRI 域反而干净
    （Anima 版反思带 category 赋义规则，这是两盘图质差异的根源）
```

**为什么 AIRI 盘是好的**：Anima 引擎的 reflectDriver 有"情感归位规则"（反思时给 category 赋义），加上 AIRI 分库 CASTALIA_PROJECT=airi 单库结构简单；Full/Lite 同源缺这一段，反思产物全走硬码旁门。

---

## 三、遗留账目（未完成事项）

### 已批准待执行（按依赖序）
1. **引擎修复一揽子**（方案已对齐，用户已拍板方向）：
   - 分库三带：总库 / conversation_log 带（is_active=0） / session_ephemeral 带（is_active=0）
   - promoteToProject 三闸（密度/归属/delta） + category 交 LLM（拆除硬码）
   - saveConversationTurn 分隔带化 + mood 前缀剥离
   - buffer 落库 + 互斥改进
   - digest per-project 锁
   - autoClassify 接线（saveMemory 分支）
   - 反思 prompt 补 category 赋义（回灌 Anima 已验证规则）
   - 一次性存量清创：备份 → 207 条 session_promoted 类别重派/软删、41 条长文压缩、11 条 AIRI 杂质隔离 → 重建向量
2. **前端六轮 commit**（v1.17.4→v1.18.3，含星图星门模型/管线）
3. **临时脚本清理**（hermes-verify-*.mjs/py × 4 + _audit_report_data.json 报告后归档）

### 待决策（阻塞中）
4. **P10 git 远程收口**：A（推荐,system 副本为正本+打齐 2 修复）/ B（本地 force-push 回写）/ C（只推 system 副本）
5. 触发探针端与用户浏览端口分离（防"探针退出杀服务"复发）——已同意，待做

---

## 四、数据附录说明

`_audit_report_data.json`：19 组（盘×库×memType）× {n, session_promoted, airi_mentions, long600}。
可复查任何一条：各库路径 `D:\AI\{lobehub-run | castalia\run\Castalia | castalia\run\Castalia-Anima}\memory\<lib>\<memType>\memory.sqlite`。

审计方法记录（可复现）：
- 源码：git grep + 逐函数读（本轮全部关键函数已打印在会话记录）
- 数据：sqlite3 只读 mode=ro，零写入
- 星图行为：puppeteer-core + 系统 Edge headless，DOM/runtime/dblcheck 三层探针
